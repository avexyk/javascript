// Creación de cadenas
var pais = "México";
var comida = new String("Ceviche");

// Medir cadena de texto
var mensaje = "";
console.log("Longitud: " + mensaje.length);

// Búsqueda: IndexOf, lastIndexOf, search, searchER
var mensaje = "Javascript en VS";
var resultado;

// Búsqueda de caracteres: match, substr, substring, charAt
var mensaje = "Javascript en VS";
var resultado;
resultado = mensaje.match(/VS/gi);

// Búsqueda de cadenas expecíficas
// startsWith, endsWith, includes

// Métodos de generación y reemplazo
// repeat, replace, slice, split, trim

// Métodos de tranformación: 
// toString, toLowerCase, toUpperCase, concat

// Plantillas y literales
var variable = "Ejemplo";
var mensaje = `Cadena ${variable}`;